import * as React from "react";
import { Box } from "@mui/material";

export default function ContentPaceHolder() {
  return (
    <Box sx={{ height: "100%", width: "100%", overflow: "hidden", bgcolor: "#404048", display: "flex", alignItems: "center", justifyContent: "center" }}>
      Karte
    </Box>
  );
}
